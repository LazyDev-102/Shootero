

using Helper;
using UnityEngine;

public class ShipEffect : CharacterEffect {

    public void AddCanvas() {
        //if (gameObject.GetComponent<Canvas>() != null)
        //    RemoveCanvas();
        //Canvas newCanvas = gameObject.AddComponent<Canvas>();
        //if (newCanvas) {
        //    newCanvas.worldCamera = CameraHelper.Camera;
        //    newCanvas.sortingLayerName = GameSortingLayer.Tutorial;
        //    newCanvas.sortingLayerID = 0;
        //}
    }
    public void RemoveCanvas() {
        //var canvas = gameObject.GetComponent<Canvas>();
        //if (canvas)
        //    Destroy(canvas);
    }
    public void Revive() {

    }
}
